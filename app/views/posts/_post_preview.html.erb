<div class="media">
  <div class="media-body">
    <h4 class="media-heading">
      <%= link_to markdown_to_html(post.title), blog_post_path(@blog, post) %>
      <small>
        submitted <%= time_ago_in_words(post.created_at) %> ago
      </small>
    </h4>
  </div>
</div>
